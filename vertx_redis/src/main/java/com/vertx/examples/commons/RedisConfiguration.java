package com.vertx.examples.commons;

import io.reactivex.Single;
import io.vertx.reactivex.core.Vertx;
import io.vertx.reactivex.redis.client.RedisAPI;
import io.vertx.reactivex.redis.client.RedisConnection;
import io.vertx.redis.client.Redis;
import io.vertx.redis.client.impl.RedisClient;

public class RedisConfiguration {


//    public static RedisConnection getRedisClient(Vertx vertx) {
////        return Redis.createClient(vertx).rxConnect();
////        return Redis.createClient(io.vertx.core.Vertx.vertx()).connect().onSuccess(redisConnection -> {
////            return redisConnection;
////        });
//
//    }
}
